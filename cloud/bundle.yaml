series: focal
applications:
  kubernetes-master:
    constraints: cores=4 mem=8G root-disk=20G
    num_units: 2
  kubernetes-worker:
    constraints: cores=6 mem=18G root-disk=85G
    num_units: 3
  etcd:
    num_units: 1
    constraints: cores=3 mem=4G root-disk=20G
